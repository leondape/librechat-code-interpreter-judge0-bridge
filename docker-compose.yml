services:
  bridge:
    build: .
    image: judge0-librechat-bridge
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0  # Override to bind to all interfaces in container
      - STORAGE_TYPE=memory
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    restart: unless-stopped
